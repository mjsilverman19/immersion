import TakeoutUpload from "@/components/import/TakeoutUpload";

export const metadata = {
  title: "Import from Google Maps | Immersion",
};

export default function ImportPage() {
  return <TakeoutUpload />;
}
